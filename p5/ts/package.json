{
    "name": "Draw",
    "version": "0.1.0",
    "description": "Simple drawing tool to play with p5",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "compile": "tsc --watch",
        "run": "reload -b"
    },
    "devDependencies": {
        "@types/p5": "^1.3.3",
        "typescript": "^3.8.3"
    }
}
