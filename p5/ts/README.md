# p5 - typescript

Playing with the p5 library.

To make this work you just need to run ...

``` bash
npm install
tsc # run it with --watch in a seperate terminal
reload -b # install it first
```

You can then change `index.html` and `src/main.ts` to see what `p5`
can do and will do.
