# p5

Playing with the p5 library.

To make this work you just need to run `reload -b`.

You can then change `index.html` and `dist/main.js` to see what `p5` can do and will do.

Do **NOT** run `npm run build`, because it will overwrite `dist/main.js`.

Todo: Figure out how to make `src/main.ts` work.
